let string = `${1+1}` // formatter... JAVA에서 %d %s 등과 같은 것 - 결국 문자열을 만들어줌.
let string2 = `console.log(${1+1})`;
string2; // 문자열 자체이므로 아무일도 일어나지 않는다.
eval(string2); // 문자열을 읽고 실행해주는 eval()

/[]/ // java의 정규표현식 쓰는 법.   